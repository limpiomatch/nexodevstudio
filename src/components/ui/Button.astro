---
interface Props {
  href?: string;
  variant?: "primary" | "secondary";
  target?: string;
  rel?: string;
}

const {
  href = "#",
  variant = "primary",
  target = "_self",
  rel = "noopener noreferrer"
} = Astro.props;
---

<a
  href={href}
  target={target}
  rel={rel}
  class:list={[
    "btn",
    variant === "primary" ? "btn-primary" : "btn-secondary"
  ]}
>
  <slot />
</a>